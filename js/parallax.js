document.addEventListener("DOMContentLoaded", function () {
    var images = document.querySelectorAll('.banner-image');
            new simpleParallax(images, {
                scale: 1.5
            });
});
document.addEventListener("DOMContentLoaded", function () {
    var images = document.querySelectorAll('.banner-image2');
            new simpleParallax(images, {
                scale: 1.3
            });
});